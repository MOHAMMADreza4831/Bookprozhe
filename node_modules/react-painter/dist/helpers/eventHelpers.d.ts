import { SyntheticEvent } from 'react';
declare const extractOffSetFromEvent: (e: SyntheticEvent<HTMLCanvasElement, Event>, scalingFactor: number, canvasRef: HTMLCanvasElement) => {
    offsetX: number;
    offsetY: number;
};
export { extractOffSetFromEvent };
